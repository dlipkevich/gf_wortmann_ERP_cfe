
&После("ОбработкаПроведения")
Процедура гф_ОбработкаПроведения(Отказ, РежимПроведения)
	
	Движения.гф_НаценкиЗаКурс.Записывать = Истина;
	
	Для Каждого ТекСтрокаНаценки Из Наценки Цикл
		
		Движение = Движения.гф_НаценкиЗаКурс.Добавить();
		
		Движение.Период			= Дата;
		Движение.Сезон			= Сезон;
		Движение.КурсДо			= ТекСтрокаНаценки.КурсДо;
		Движение.ПроцентНаценки	= ТекСтрокаНаценки.ПроцентНаценки;
		
	КонецЦикла;

КонецПроцедуры
