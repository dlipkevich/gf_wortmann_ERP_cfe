
&НаСервере
Процедура ПроверитьНаСервере()
	
	Попытка
		база = Справочники.гф_УзлыОбменаПоВозвратам.ПодключитьсяКБазе(Объект.Ссылка);
	Исключение
		Текст = ОписаниеОшибки();
		Сообщить(Текст);	
	КонецПопытки;
	Если база <> Неопределено Тогда
		Сообщить("Соединение успешно установлено !");	
		база = Неопределено;	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Проверить(Команда)
	ПроверитьНаСервере();
КонецПроцедуры
