
#Область ПрограммныйИнтерфейс   


Функция ЗначениеДополнительнойНастройки(Идентификатор, ВместеСХранилищем) Экспорт 
		
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	БФД_ДополнительныеНастройки.Идентификатор КАК Идентификатор,
		|	БФД_ДополнительныеНастройки.Значение КАК Значение,
		|	БФД_ДополнительныеНастройки.ЗначениеХранилище КАК ЗначениеХранилище
		|ИЗ
		|	РегистрСведений.БФД_ДополнительныеНастройки КАК БФД_ДополнительныеНастройки
		|ГДЕ
		|	БФД_ДополнительныеНастройки.Идентификатор = &Идентификатор";
	
	Запрос.УстановитьПараметр("Идентификатор", Идентификатор);	
	РезультатЗапроса = Запрос.Выполнить();	
	Выборка = РезультатЗапроса.Выбрать();
	
	Если Выборка.Следующий() Тогда		
		СтруктураВозврата = Новый Структура("Значение", Выборка.Значение);		
		Если ВместеСХранилищем Тогда
			СтруктураВозврата.Вставить("Хранилище", Выборка.ЗначениеХранилище.Получить());
		КонецЕсли;		 
		Возврат СтруктураВозврата				
	Иначе  		
		Возврат Неопределено;		
	КонецЕсли;
	
КонецФункции


#КонецОбласти